def solution(food):
    answer = ''
    rev_ans=''
    for i in range(1, len(food)):
        answer += (food[i] // 2) *str(i)

    rev_ans = ''.join(reversed(answer))
    answer = answer +'0' +  rev_ans
    return answer